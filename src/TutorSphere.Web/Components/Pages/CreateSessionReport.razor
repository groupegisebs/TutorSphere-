@page "/tutor/reports/create"
@layout TutorLayout
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
        => Navigation.NavigateTo("/tutor/reports", replace: true);
}
