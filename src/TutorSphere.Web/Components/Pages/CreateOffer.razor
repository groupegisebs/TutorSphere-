@page "/tutor/offers/create"
@layout TutorLayout
@rendermode InteractiveServer
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
        => Navigation.NavigateTo("/tutor/calendar?createOffer=1", replace: true);
}
