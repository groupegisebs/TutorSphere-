@page "/tutor/premium"
@layout TutorLayout
@rendermode InteractiveServer
@inject NavigationManager Navigation

@* Ancienne page multi-forfaits : l'activation unique est à 10 $ USD / an. *@
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/tutor/activate", replace: true);
    }
}
