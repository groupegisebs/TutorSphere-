@page "/parent/invoices/{Id:guid}"
@layout ParentLayout
@inject NavigationManager Nav

@code {
    [Parameter] public Guid Id { get; set; }

    protected override void OnInitialized() =>
        Nav.NavigateTo($"/parent/payments?invoice={Id}", replace: true);
}
